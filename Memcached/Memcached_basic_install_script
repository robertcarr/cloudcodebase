#!/bin/bash
#

$MEMCACHED_MAX_MEMORY
$MEMCACHED_PORT
$MEMCACHED_IP_ADDRESS

memcached -d -m $MEMCACHED_MAX_MEMORY -l $MEMCACHED_IP_ADDRESS -p $MEMCACHED_PORT
logger -t RightScale "Memcached started with $MEMCACHED_MAX_MEMORY PORT $MEMCACHED_PORT on IP $MEMCACHED_IP_ADDRESS

